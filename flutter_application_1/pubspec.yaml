name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  animated_splash_screen: ^1.3.0
  google_fonts: ^6.0.0
  carousel_slider: ^5.0.0
  image_picker: ^1.0.7
  path_provider: ^2.0.15 # <-- Added for local storage

  # Firebase dependencies can be kept if you still use authentication or Firestore
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.6.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/my_assets.png
    - assets/images/image1.jpg
    - assets/images/image2.jpg
    - assets/images/image3.jpg
    - assets/images/image4.jpg
    - assets/images/image5.jpg
    - assets/default_avatar.jpg # <-- ensure the extension matches your asset file
